/**
 * 全局 Dmall 实例
 * @namespace Dmall
 */
var Dmall = {
  /**
   * foo方法
   * @memberof Dmall
   * */
  foo() {

  },
  bar() {

  },
  /**
   * 版本号
   * */
  version: "1.0.0"
}

/**
 * 加法函数
 * @since 3.0.0
 * @category Array
 * @memberOf Dmall
 * @param {number} a - 加数
 * @param {number} b - 被加数
 * @return {number} 值
 * @example
 *
 * plus(1,1) => 2
 *
 * */
function plus(a, b) {

}

Dmall.plus = plus